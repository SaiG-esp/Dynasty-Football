# Frontend

React + Vite SPA for Dynasty Scout. See the [root README](../README.md) for architecture, local setup, and environment variables.

```bash
npm install
cp .env.example .env
npm run dev      # http://localhost:5173
npm test
npm run lint
```
